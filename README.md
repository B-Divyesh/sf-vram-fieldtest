# VRAM Field Test

Live: https://vram-fieldtest.sociobot.in — built by the Param Factory (`cli-installers`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
