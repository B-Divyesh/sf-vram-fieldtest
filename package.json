{
  "name": "vram-fieldtest-site",
  "private": true,
  "version": "0.1.9",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:site": "node site/build.mjs",
    "build": "npm run build:site && cargo build --release",
    "test": "node site/test-runner.mjs",
    "test:browser": "playwright test",
    "lint": "node --check site/src/app.js && node --check site/build.mjs && node --check site/dev.mjs && node --check site/live-verify.mjs && node --check api/license-verify/index.js && python3 -c \"import ast,pathlib; ast.parse(pathlib.Path('scripts/hardware-evidence.py').read_text())\" && sh -n site/public/install.sh && cargo fmt --check && cargo clippy --locked -- -D warnings",
    "verify:live": "node site/live-verify.mjs",
    "dev": "node site/dev.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "playwright-core": "1.58.2"
  }
}
